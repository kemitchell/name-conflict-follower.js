An [npm registry follower][follower] that lists package names and
responds to HTTP requests for similar and potentially confusing
conflicts.

[follower]: https://github.com/npm/registry-follower-tutorial
